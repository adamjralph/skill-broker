# Skill Broker handoff

Updated: 2026-09-21 14:12 AEST (verified against the working tree, Git, GitHub, the store, and the live profile)
Next objective: **Stage 7 sign-off** ([#60](https://github.com/adamjralph/skill-broker/issues/60)) — live Jev now works (direct HTTP, key present) and `judgment: live` is set on `stillroom-signal-generator`, so a fresh Shadow window is accruing the extra Cases the threshold split needs (19 threshold-side today, 11 reviewed). Once the threshold side reaches 20: review the remaining queue (15 of 27 already reviewed; 12 pending) → `evaluate.py register` → build and review the Shadow Report, which is the thing that enables the batch. **Live measured expansion** ([#58](https://github.com/adamjralph/skill-broker/issues/58)) is built and rehearsed; its live leg waits on #60. **Stage 9 retirement is now defined** ([#23](https://github.com/adamjralph/skill-broker/issues/23), ADR-0023, [`docs/retirement/`](retirement/README.md)) though its execution is blocked until every Consumer resolves through the store. The bounded pilot **[#57](https://github.com/adamjralph/skill-broker/issues/57)** is closed: rehearsed deterministically end to end (`tests/hermes_adapter/run_pilot_proof.py`, `VERDICT: PASS`) and executed live for `stillroom-signal-generator`, where a real turn received the Pack — with two recorded deviations (deterministic `first_candidate` judgment instead of live Jev, and no reviewed Shadow Report). The withholding decision **[#53](https://github.com/adamjralph/skill-broker/issues/53)** (ADR-0021), the Hard Gates and per-batch switch **[#52](https://github.com/adamjralph/skill-broker/issues/52)**, the Shadow Report and batch review **[#56](https://github.com/adamjralph/skill-broker/issues/56)**, the project-setup fix **[#59](https://github.com/adamjralph/skill-broker/issues/59)** and the expansion procedure and its second batch **[#58](https://github.com/adamjralph/skill-broker/issues/58)** are built.

## Start here

Suggested skills, in order: `implement` → `tdd` (at the Hermes seam) → `code-review`.
Consult `domain-modeling` before touching `CONTEXT.md`, and the repo's agent docs:

- [`AGENTS.md`](../AGENTS.md) — project rules, graft usage, issue-tracker/triage/domain doc pointers
- [`CONTEXT.md`](../CONTEXT.md) — the domain language; names in code must match it
- [`docs/agents/issue-tracker.md`](agents/issue-tracker.md) — `GH_CONFIG_DIR=~/.config/gh-personal` for every `gh` call; claim/close conventions
- [`PROJECT-OUTLINE.md`](../PROJECT-OUTLINE.md) — the outline of record

Authoritative spec and decisions:

- issue **#44** — the accepted spec (store half built; broker half in progress). It carries the stage map, the `B1`–`B17` implementation decisions, and the testing seams.
- ADRs in [`docs/adr/`](adr/) — the ones that bind the broker: [`0001`](adr/0001-one-external-seam-two-internal-seams.md) (one external seam, two internal), [`0004`](adr/0004-jev-judges-relevance-code-grants.md), [`0006`](adr/0006-v1-judgment-is-single-primary-plus-dependencies.md), [`0010`](adr/0010-pack-delivery-under-the-hook-cap.md), [`0013`](adr/0013-candidate-retrieval-is-lexical-and-jev-returns-one-choice.md), [`0014`](adr/0014-authority-is-per-turn-and-the-session-ledger-is-not-a-lease.md), [`0015`](adr/0015-the-evidence-log-records-metadata-and-hashes-not-request-text.md), [`0018`](adr/0018-profile-policy-is-authored-in-the-store-and-manifests-are-derived.md), [`0019`](adr/0019-injection-gate-is-precision-first-and-fails-closed.md), [`0020`](adr/0020-the-replay-corpus-is-opted-in-local-and-hand-reviewed.md), [`0021`](adr/0021-brokered-withholding-is-a-cutover-disabled-list-edit.md), [`0023`](adr/0023-retirement-is-archive-first-and-explicitly-approved.md) (retirement is archive-first; deletion is a separate purge).
- For code context, use the graph first: `graft map`, `graft ask "<question>" --source`, `graft callers <symbol>`. Refresh with `graft build` after changes.

## Verified current state

**Git.** Branch `main`, pushed to `origin/main`. Recent commits: this-session `record --live` for a pre-broker Case + `request_sha256` backfill (#60, HEAD), then the handoff commit; earlier `73caacd`/`364e68f` #58 hardening, `66b5e1a` live Shadow Report join + `record --from-evidence`, `6fd308d` direct-HTTP Jev, `8fef19b` live smoke, `9797358` review queue, `e3015e6` split caveat.

**Tests.** `python3 -m unittest discover -s tests` from the repo root → **440 tests, all passing**. No packaging, no third-party deps beyond PyYAML for the store tooling. The executed Hermes-seam proofs are separate (they need the Hermes venv): `~/.hermes/hermes-agent/.venv/bin/python tests/hermes_adapter/run_shadow_proof.py` → `VERDICT: PASS`, and `~/.hermes/hermes-agent/.venv/bin/python tests/hermes_adapter/run_pilot_proof.py` → `VERDICT: PASS`. The expansion proof runs with the repository Python over the real store: `python3 tests/hermes_adapter/run_expansion_proof.py` → `VERDICT: PASS`.

**Broker modules** (`scripts/broker/`): `broker.py` (`Broker.prepare_turn`, the only external seam), `closure.py` (`authorised_closure`, `dependency_closure`), `retrieval.py` (BM25 candidate ranking), `metadata.py` (frontmatter retrieval metadata), `judgment.py` (`JudgmentCall`, sources, `validate`, `grant`, `FallbackJudgmentSource`, `FirstCandidateJudgmentSource`, SHA-bound `Recording`), `jev.py` (`JevJudgmentSource`, `live_judgment_source`), `pack.py` (`HookConfig`, `Budget`, `build_pack`, Pack Delivery), `evidence.py` (`EvidenceLog`, `JsonlEvidenceLog`, `SessionLedger`, `append_jsonl`), `types.py` (`RouteDecision`, `InterventionResult`, `PackDelivery`, …), `adapter.py` (the Hermes Adapter and its API-request evidence handle), `corpus.py` (Replay Corpus extraction, the machine-local Case store, the committed reviewed-label and Recording stores), `evaluation.py` (offline replay over Reviewed Cases and Recordings, per-split metrics, retrieval/Judgment/Grant attribution, the committed pre-registration of Soft Thresholds, and `soft_threshold_regression`), `gate.py` (the Hard Gates, fail-closed Incidents, the per-batch switch, and the prompt/tool-schema baseline), `report.py` (the Shadow Report and its batch review), `hermes_plugin/` (the loadable `skill-broker` plugin). The CLIs are `scripts/corpus.py`, `scripts/evaluate.py`, `scripts/gate.py` and `scripts/shadow_report.py`.

**Closed tickets.** #45–#55 (module home, seam, retrieval, Judgment/Grant, Pack, live Jev, Hermes Adapter in Shadow Mode, Replay Corpus and Reviewed Cases, offline routing evaluation and pre-registered Soft Thresholds) #52 (Hard Gates, fail-closed, the per-batch injection switch), #53 (Brokered withholding decided and implemented, ADR-0021), #56 (the Shadow Report and batch review), #57 (the bounded intervention pilot, rehearsed), #58 (measured expansion, the procedure and its second batch), #59 (the project-setup skills block) and #23 (the Stage 9 retirement process, ADR-0023).

**Skill Store.** `~/skill-store` (repo `adamjralph/skill-store`): **415 identities**, manifest verifies clean; two policies `life-agent.json` and `stillroom-signal-generator.json`. Verified this session:

```
python3 scripts/store_manifest.py verify --store ~/skill-store      # ok
python3 scripts/profile_policy.py validate --store ~/skill-store    # 2 policies
```

**Cutover.** `life-agent` is live, zero-delta, rollback rehearsed; farm at `~/.local/share/skill-broker/farms/life-agent`. Brokered withholding is a Cutover `skills.disabled` edit (ADR-0021): a pilot rehearsal applied the five Brokered Names to a byte-copy of `stillroom-signal-generator`'s config, verified the gate, and rolled the config back byte-for-byte.

**Open frontier**: **#60 Stage 7 sign-off** is in progress — live-Jev Shadow window accruing, review queue ready; #58's live leg is blocked behind #60; the wayfinder map #1 stays open. Stage 9 retirement **execution** stays blocked until every Consumer resolves through the store.

**Evidence from #23.** Stage 9 retirement is defined, not executed: [ADR-0023](adr/0023-retirement-is-archive-first-and-explicitly-approved.md) and [`docs/retirement/README.md`](retirement/README.md) split the irreversible act in two — **Retire** is a reversible, batched, manifest-backed move gated on Adam's recorded approval; **Purge** is the separate, later, explicitly-approved deletion after a retention window. The review artifact is a committed `docs/retirement/<batch>.json` plus `report.md`, generated from the verified Store Manifest and Stage 1 audit index (digests cited), one row per candidate (redundant-copy / dead-exposure / excluded-root / zero-use-stale) with its resolution, hash, curator usage evidence and reference check. A deterministic `scripts/retire.py propose` proposes; a model never does. The curator keeps sole ownership of its managed roots (it already archives, never deletes; `hermes curator purge` is its deletion path), Stage 9 never touches `.archive/`/`.curator_backups/`, and a curator-archived store identity is already-withdrawn evidence. A batch is blocked by an uncut Consumer, an open Incident, a failing manifest/policy or stale cited digest, an unresolved/unverified candidate, use inside the retention window, any dependency/cross-skill/cron/pin reference, or an unmet licence/NOTICE obligation. Execution stays blocked until every Consumer resolves through the store. `scripts/retire.py` is not built yet — the first real batch builds it.

**Evidence from #51.** The Adapter is built (`scripts/broker/adapter.py`) and loadable as the `skill-broker` Hermes plugin (`scripts/broker/hermes_plugin/`). The executed Hermes-seam proof runs five modes against a mock provider under one isolated `HERMES_HOME` and passes: byte-identical system prompt and tool schema control-vs-treatment and across turns, no Pack on the wire in Shadow Mode while a Pack is built, one evidence record per provider request locating its Route Decision, multimodal and `codex_app_server` refused with the reason recorded, an Adapter failure swallowed, and the effective hook cap read from config (which is unchanged). Injection remains **off** by default.

**Evidence from #57.** `tests/hermes_adapter/run_pilot_proof.py` rehearses the whole pilot lifecycle with no live Consumer state touched. The Cutover runs on a byte-copy of `stillroom-signal-generator`'s `config.yaml`: the farm is wired into `skills.external_dirs`, the five Brokered Names are withheld in `skills.disabled`, the policy gate is green, and rollback restores the config byte-for-byte. Shadow Mode runs real Hermes turns under an isolated `HERMES_HOME` against the **real** Skill Store and policy with a mock provider, building a Pack with nothing on the wire. A Shadow Report assembled from those recorded Route Decisions is approved, enabling exactly one batch (`stillroom-signal-generator-brokered`); the same seam with the batch enabled steers a real turn with a Pack carrying `coreyhaines31.cold-email`, keeps the system prompt and tool schema byte-identical to the plugin-disabled control, suppresses the repeated turn, and locates every provider request's Route Decision from the Evidence Log; the batch is then disabled back to foundation-only. `VERDICT: PASS`. `tests/test_pilot_proof.py` (7 tests) exercises the proof's acceptance logic so a broken checker cannot pass silently.

**Evidence from #58.** `scripts/broker/expansion.py` encodes the measured-expansion routine (ADR-0022): `assert_expandable` fails closed on an open Incident, a failed-closed profile, a Soft-Threshold block, or no running batch to grow from; `assert_fresh_review` refuses a review that reuses the previous Report digest or predates the previous review; `assert_superset` keeps growth additive; `verify_thresholds` holds the pre-registered thresholds or accepts a re-derivation only against a strictly larger reviewed corpus (delegating to `ThresholdRegistry`, which records the new `corpus_sha256` and baseline); `rehearse_rollback` disables the running batch, observes foundation-only and restores it before anything is enabled; `check_after_batch` reads the post-batch window through the same `check_hard_gates` and reports foundation-resolution regressions and broker/native hash agreement; and `expand` records the whole step to an append-only expansion ledger, fails the profile closed on a post-batch breach, and withdraws the new batch and restores the previous exposure when the post-batch probe cannot run. A review is bound to the batch it enables: `apply_review` refuses an approval whose supplied batch does not cover exactly the reviewed Skills, and `expand` only ever approves (a narrowing review is not a growth step). `gate.review_closes_incident` is the one home for the "a review closes an Incident only if recorded strictly after it" rule that `open_incidents` and `enable` share. `InjectionGate.open_incidents` is the new durable notion of an Incident no later review closed. `scripts/expand.py` exposes `status`, `rehearse` and `apply` for the operator (apply records the review through `apply_review`, so the reviewed Shadow Report is the only thing that enables a batch). `tests/hermes_adapter/run_expansion_proof.py` (`VERDICT: PASS`) routes over the **real** Skill Store through a verifying symlink overlay and authors the expanded policy in that overlay: it enables the pilot batch (`stillroom-signal-generator-brokered`, 5 Skills) on a reviewed report, grows the policy by three newly-brokered stillroom workflows, assembles a second report from a fresh window and reviews it with its own digest, runs the procedure (fresh review, additive batch, thresholds held, rollback rehearsed, post-batch checks green), proves the pilot's cold-email request still grants `coreyhaines31.cold-email` and delivers the byte-identical Pack with the same hash, rolls the second batch back to the first exposure, and refuses expansion once the profile is failed closed. `tests/test_broker_expansion.py` (32 tests) and `tests/test_expansion_proof.py` (7 tests) cover the procedure and its acceptance logic. `docs/runbooks/expansion.md` records the routine.

**Evidence from #60.** Two live-path blockers for Stage 7 sign-off are fixed. (1) The Shadow Report joined a recorded Route Decision to a session turn by `(session_id, turn_id)`, but a live decision's `turn_id` is Hermes's ephemeral per-turn correlation id (`session:task:8hex`), never the `state.db` message id — so every live decision was dropped as `turn_not_in_window` and the report was empty. `build_shadow_report` now joins by `(session_id, request content hash)`, consuming matches in log order so a repeated request resolves to its own turn. Verified live: the pilot window went from 0 turns / 5 `turn_not_in_window` problems to 5 turns / no problems. (2) `scripts/corpus.py record --from-evidence` freezes the `judgment` the live broker already recorded in `route_decisions.jsonl`, joined to the Case by the request's content hash (the Case now records the unredacted `request_sha256` alongside the redacted `case_sha256`); it refuses a non-live-Jev source unless `--any-source` is passed. `scripts/broker/corpus.py` gains `decision_for_case`/`claim_from_decision`; `tests/test_broker_report.py` (3) and `tests/test_broker_corpus.py` (7) cover both. Extraction yields 7 Cases without extra consent; Adam consented to the held-back sources on 2026-09-21, taking the corpus to **27 Cases**. (3) `broker/jev.py` no longer needs `typesafe_sdk`: the default client is a direct, single-attempt HTTP call to TypeSafe System One, so a Hermes venv rebuild cannot prune it. The key is resolved through Hermes's per-turn `agent.secret_scope` (a routed gateway/desktop/kanban turn keeps credentials out of `os.environ`), falling back to the environment outside Hermes; a refused scope never leaks the process env. `tests/test_broker_jev.py` covers the scope resolution, the refusal, the key requirement, the one bounded POST, and the wire shape. (4) `scripts/corpus.py queue` writes a machine-local, pre-labelled review queue (`~/.config/skill-broker/corpus/queues/<profile>.md`); `--write-prelabels` stores each suggestion on its Case. The pilot profile's queue is generated: 27 Cases (split **threshold 18 / held-out 9**), live Jev suggestions `no_skill` 20 / `life-os.stillroom-writing-workflow` 6 / `coreyhaines31.cold-email` 1, in 26s. **Supply caveat:** `evaluate.py register` needs the threshold-setting side to reach 20 (`threshold.cases < minimum` in `evaluate_profile`), and all 27 Cases give only 18 — so ~5–8 more live Cases are needed before the review can pre-register thresholds. (5) **#60 (a) is in progress:** the `skill-broker` plugin config on `stillroom-signal-generator` now sets `judgment: live` (backup `config.yaml.bak-20260921-140901`); `inject` stays false and the gate shows `enabled_batch: null`, so real turns record live-Jev Route Decisions with nothing on the wire. Use the profile to accumulate the extra Cases, then re-extract. (6) **Recording caveat — re-verified this session.** `record --from-evidence` cannot freeze the pilot corpus the runbook assumed it would: the 27 Cases are pre-broker turns the live broker never judged, and the only recorded Decisions in `route_decisions.jsonl` are four `no_skill` (session `20260921_115202`) and one `first_candidate` (session `20260921_120548`) — none answered by live Jev — so every Case was unfreezable and **zero Recordings exist**. Two fixes: `extract_cases` now backfills `request_sha256` on a Case extracted before the hash existed instead of counting it a duplicate (`decision_for_case` tells the operator to re-extract, and now that actually works; 7 real Cases backfilled), and `scripts/corpus.py record --live` asks live Jev for one Case through the real `Broker` and freezes that Choice, refusing any fallback source so a No-Skill floor is never frozen as a live Choice. Verified live: `--live` on the pilot request froze `coreyhaines31.cold-email` (confidence 1.0). `tests/test_broker_corpus.py` (+3: 1 backfill, 2 record-live). After the backfill, 3 of 27 Cases join a recorded Decision and all three are `no_skill`, so the recordings must come from `--live`. **Recordings frozen this session:** all 27 pre-existing Cases have a committed Recording in `corpus/recordings/stillroom-signal-generator/` (commit `4487f96`), every one binding; distribution `no_skill` 20 / `life-os.stillroom-writing-workflow` 6 / `coreyhaines31.cold-email` 1, matching the queue's pre-labels. A Recording carries only the Case hash and the Choice — no request text. **Labels written this session:** **16 of 28** reviewed (`corpus/labels/stillroom-signal-generator.json`, commits `0c09bbe`/`c37da99`): agent-drafted from the queue and Adam-approved, **threshold 11 / held-out 5**. One correction against the prelabel — `162a1277` ("tell me what your role is") is `no_skill`, not `stillroom-writing-workflow`. **12 stay pending:** the 11 kanban-source Cases (10 bare `work kanban task t_x` triggers + one operator note) and `46fe0fcf` (an article-setup turn where the Chief is still asking Adam for a topic) — whether a bare kanban trigger warrants a Skill depends on the Workflow Generator's eventual skill routing (`~/Projects/workflow-generator`, open §3.1), and ADR-0020 bans observed `skill_view` use as a label. **Live Jev verified through the seam at last (commit `c37da99`):** a real routed turn ("write a cold email to a prospect introducing our offer") recorded `judgment_source: jev`, `granted`, a `coreyhaines31.cold-email` Grant and a 7,988-char Pack with injection off — nothing on the wire — and one Evidence record locating the Decision. That turn is the new **28th Case** (threshold side), recorded from the Decision with `--from-evidence` (not a re-Jev) and labelled `coreyhaines31.cold-email`; gate `injecting: false`, 0 Incidents. **Offline replay runs clean:** `evaluate.py evaluate` reports 16 reviewed, no `recording_missing`, threshold precision/recall **1.0** on 11 Cases, held-out precision **0.75** (the one `false_intervention` is `162a1277`), so the recordings and labels bind. **Supply still short:** the threshold split is now **19** Cases and `threshold_reviewed` is **11 of 20**, so `register` still refuses — labelling the 8 pending threshold Cases reaches 19 and then needs ~2 more live turns.

Executed live for `stillroom-signal-generator` (2026-09-21): a real turn's Route Decision recorded `granted` with a `coreyhaines31.cold-email` grant and `delivery: inline` (7,988 chars, `pack_sha256` set), the gate stayed injecting with zero Incidents, and rollback restored the config byte-for-byte. Two deviations are recorded on the issue: judgment used `first_candidate` because `typesafe_sdk` is absent from the Hermes venv (live Jev fell back to No-Skill), and the batch was enabled with `scripts/gate.py enable` without a reviewed Shadow Report or pre-registered thresholds.

**Evidence from #54.** `scripts/broker/corpus.py` implements extraction from a Hermes `state.db` (`read_turns`), redaction of secrets/identifiers at extraction, the machine-local `CorpusStore` (refusing a root inside the repo, and refusing any source not opted in), the committed `LabelStore` (hand-corrected `no_skill` or closure-authorised Primary only), the SHA-bound `RecordingStore` bound to the Case by content hash, the disjoint threshold/held-out `split_for`, `profile_status` against the provisional Stage 5 minimum, and `retention_report` for the Stage 5/6 review. `scripts/corpus.py` exposes `extract`/`status`/`retention`/`review`/`prelabel`/`record`/`queue`/`purge`. A real-machine smoke run extracted the pilot `stillroom-signal-generator` into a throwaway corpus: 4 Cases (cli/tool), 4 redactions, refused `desktop` (9) and `kanban` (14); the store/root DBs are never pooled and an untagged turn belongs only to the default profile. No real corpus content or labels were created.

**Evidence from #52.** `scripts/broker/gate.py` implements the injection gate. `check_hard_gates` is the data-independent check over a Route Decision — an unauthorised Grant, a foundation-resolution regression, broker/native hash disagreement, a Judgment validation failure and an incomplete Dependency Closure — and `InjectionGate` is the durable per-batch switch: `enable(batch, review)` refuses a review that does not name the batch or does not post-date a breach, `fail_closed` records an append-only Incident, turns injection off and marks the profile review-required, and `check_soft_thresholds`/`note_expansion_regression` block expansion while leaving a running injection on. `Broker` consults the gate on every turn: a breach reverts the turn to foundation-only and records the Incident; a failed-closed profile short-circuits to `injection_failed_closed`. The Adapter consults `gate.injecting` before delivering (the per-batch switch is authoritative over the legacy `inject` flag) and checks the system-prompt hash, the tool count and the tool-schema hash against the conversation baseline on every provider request, failing the profile closed on a mutation. `scripts/gate.py` exposes `status`/`enable`/`disable`/`regression`/`clear-regression`/`incidents`. `soft_threshold_regression` (quality floors only) lives in `gate.py` and is re-exported from `evaluation.py`. 41 tests in `tests/test_broker_gate.py`.

**Evidence from #56.** `scripts/broker/report.py` implements the Shadow Report. `build_shadow_report` joins the recorded Route Decisions from the Evidence Log with the `skill_view` use read from the profile's `state.db` (`corpus.read_skill_use`), windows on the session turn's timestamp, and assembles a `ShadowReport`: the profile, the exact Brokered Allowlist as the `InjectionBatch` that enabling would cover, each Hard Gate's status via `check_hard_gates` plus the Incident log, movement against the pre-registered Soft Thresholds via `soft_threshold_regression`, and every turn where the broker differed from actual use or human review with the retrieval/Judgment/Grant attribution. `apply_review` is the only path to `InjectionGate`: approve enables the report's batch, `approve_narrower` enables a proper subset of it, and reject records the review and enables nothing, leaving Shadow Mode running. An approval is refused unless the Soft Thresholds are pre-registered, every checked Hard Gate passed, and no quality floor regressed (a regression also records the expansion block). The review is bound to the report by `report_sha256`. `scripts/shadow_report.py` exposes `build`/`show`/`review`. 27 tests in `tests/test_broker_report.py`.

**Evidence from #55.** `scripts/broker/evaluation.py` replays every Reviewed Case through the real `Broker` against its bound `Recording` with no network and no model, reporting per profile and per split: precision, recall, correct no-skill rate, unauthorised-grant rate, closure/dependency/cycle failures, average Candidate count, average Pack size, duplicate-injection rate and hash agreement, plus retrieval/Judgment/Grant attribution so a wrong outcome is explained. The held-out set is reported separately and `derive_thresholds` reads only the threshold set. `ThresholdRegistry` pre-registers the Soft Thresholds in `corpus/thresholds/<profile>.json`, bound to a digest of the reviewed corpus + Recording claims and the measured baseline, refusing a move unless re-derived against a strictly larger reviewed set. `below_minimum` requires the threshold-setting set itself to reach the Stage 5 minimum, so a profile with enough total cases but too few tuning cases stays in Shadow Mode. `scripts/evaluate.py` exposes `evaluate`/`register`. A real-machine smoke run against the empty pilot corpus wrote nothing and reported `stage: shadow`.

## In progress and pending

**#60 (a) is in progress, nothing half-written.** Live Jev works and `judgment: live` is set, so the next work is to accumulate and review evidence:

- **Accumulate** — use `stillroom-signal-generator` normally until the corpus split has ≥20 threshold-side Cases (today 18 of 27); then re-extract (`scripts/corpus.py extract --profile stillroom-signal-generator --consent desktop --consent kanban`).
- **Review** — 15 of 27 Cases are reviewed (agent-drafted, Adam-approved); 12 stay pending (11 kanban-source + `46fe0fcf`). Queue: `~/.config/skill-broker/corpus/queues/stillroom-signal-generator.md`; draft sheet: `...-draft.md`. Recordings are frozen 27/27, so once the pending labels land and ≥20 threshold-side reviewed Cases exist, run `scripts/evaluate.py evaluate`/`register`.
- **Enable** — build + review the Shadow Report (`scripts/shadow_report.py build`/`review`); the review is the only thing that enables the pilot batch ([`docs/runbooks/live-pilot.md`](runbooks/live-pilot.md)).
- **Then #58's live leg** — grow the profile's `brokered` list (a reviewed store change, ADR-0018), `scripts/expand.py status` → `rehearse` → fresh report → `apply`.
- **Stage 9 retirement** is defined ([`docs/retirement/README.md`](retirement/README.md), ADR-0023) and blocked until every Consumer resolves through the store; `scripts/retire.py` is built with the first real batch.
- **Live state left in place:** the `skill-broker` plugin symlink and `plugins:` entry on the profile, `judgment: live` (backup `config.yaml.bak-20260921-140901`), and the earlier `config.yaml.bak-20260921-120513`. Injection is off; the gate has no enabled batch and the Cutover is rolled back. **Corrected this session:** the Cutover rollback had removed the farm from `skills.external_dirs` but left the five Brokered Names in `skills.disabled` on the live config, so the pilot proof failed (`cutover withheld [], expected [five names]`) and the Shadow window would have suppressed the very native `skill_view` use the Report compares against. The `skills:` block was removed to restore the pre-Cutover config (backup `config.yaml.bak-<ts>-pre-withhold-restore`), keeping the `judgment: live` entry, and `run_pilot_proof.py` is `VERDICT: PASS` again.

The Adapter's settings are `store`, `profile`, `evidence_dir`, `judgment` (`live` | `recorded` | `first_candidate` | `no_skill`) and `inject` (deprecated; the per-batch gate state under `evidence_dir` is authoritative). Route Decisions are appended to `<evidence_dir>/route_decisions.jsonl`; the API-request evidence handle to `<evidence_dir>/api_requests.jsonl`; the gate state to `<evidence_dir>/gate.json`; Incidents to `<evidence_dir>/gate_incidents.jsonl`.

## Decisions and boundaries

- **Injection is off by default.** Shadow Mode runs the whole pipeline at the real seam and returns no context. Enabling a batch needs a reviewed Shadow Report and Adam's explicit approval (ADR-0019).
- **Migration is additive and reversible.** No Skill, source path, or Consumer path is deleted without explicit approval; Stage 9 Retirement is not authorised.
- **Main stays docs/spec/tooling.** Prototypes live on throwaway branches. `prototype/hermes-intervention-seam` is out of main.
- **Authority.** Only deterministic code mints a Grant; Jev never grants. The Session Ledger is dedup evidence, never a lease. The Evidence Log holds metadata and hashes — no request text, no Skill bodies (ADR-0015).
- **Jev prompt/criteria engineering is carried fog** (B5, spec "Out of Scope"). #50 delivered a working, untuned source; do not tune it here.
- **B16 is resolved (ADR-0021):** Brokered withholding is a Cutover edit to the Consumer's `skills.disabled`; hard-gate item 5′ is checked per batch at Cutover, not per turn. The Adapter keeps its no-write, no-index hook surface. The Adapter's packaging/distribution/enablement stays carried fog, now unentangled with withholding.
- **Falsification condition.** If Reviewed Cases require two independent primaries, ADR-0006's single-primary deferral is lifted by a new ticket, not a silent change.
- **Uncommitted local state — leave it:** `.gitignore` and `AGENTS.md` carry graft additions; `.ignore`, `opencode.json` and `prototypes/` are untracked. Not part of any ticket. Do not commit or clean without Adam.

## Blockers and troubleshooting

- **Live Jev** — the default client is a direct HTTP call (`jev.py::_http_client`, `POST https://api.typesafe.ai/v1/systemone`), so the Hermes venv needs no `typesafe-sdk` package that a venv rebuild would prune. `TYPESAFE_API_KEY` is in both `~/.hermes/.env` and `~/.hermes/profiles/stillroom-signal-generator/.env` (backups `*.bak-20260921-133825`). **Fixed this session:** `_http_client` read `os.environ` only, but a *routed* Hermes turn (gateway / desktop / kanban) installs the profile's credentials as a per-turn `agent.secret_scope` scope instead of writing them to `os.environ` — so live Jev would have missed the key and silently fallen back to No-Skill on exactly the turns the Shadow window observes. `_api_key()` now resolves through `agent.secret_scope.get_secret` (lazy Hermes-internal import) with an environment fallback outside Hermes, and a refused scope is a missing key, never an env read. Verified against the real scope: multiplex + profile scope resolves the key (len 108), no scope fails closed. Test through `client_factory`; a real live call is optional evidence, not an AC. **Verified live 2026-09-21**: `source: jev`, a well-formed Choice and usage in ~0.74s.
- **Jev timeout budget (fixed in #50, keep it fixed).** One attempt per turn, bounded by `DEFAULT_TIMEOUT = 8.0s`, well under Hermes's 30s `plugins.hook_callback_timeout`. The HTTP client makes exactly one POST. Keep `tests/test_broker_jev.py::test_the_http_client_posts_one_bounded_request` green.
- **Hook timeout / cap facts.** `plugins.hook_callback_timeout` default 30s, max 600, `0` disables bounding. `hooks.output_spill.max_chars` default 10,000, `preview_head`/`preview_tail` 500 each, `enabled: false` disables. Hermes abandons a timed-out `pre_llm_call` worker and continues (fail-open).
- **Unsupported delivery paths** (research: [`docs/research/hermes-delivery-paths.md`](research/hermes-delivery-paths.md)): ordinary text is supported end to end; multimodal user content and `codex_app_server` drop the injected sidecar; MoA carries it but participant recovery is unclaimed; a context engine that replaces the request is unknown. Unsupported paths must emit no Intervention.
- **Store smoke test without the full suite:** build a `Broker(store=~/"skill-store", evidence_log=RecordingEvidenceLog(), judgment_source=StubJudgmentSource(claim))` and call `prepare_turn("…", "life-agent", {"session_id": "smoke"})`. A real `life-agent` grant produced an 8,257-char inline Pack.

## Next actions

1. **#60 (a), in progress** — supply: the corpus has 28 Cases, threshold split **19**, reviewed **16** (threshold 11). Label the 8 pending threshold Cases (kanban rule + `46fe0fcf` need Adam's decision, see the Workflow Generator), then use `stillroom-signal-generator` for ~2 more turns to clear 20, re-extract, review those too, then `scripts/evaluate.py evaluate`/`register`. Recordings are frozen for the 27 pre-broker Cases and the 28th came from live evidence.
2. **#60 (b)** — build and review the Shadow Report (`scripts/shadow_report.py build`/`review`); the review enables the pilot batch. Verify the Hard Gates, hash agreement, byte-identical prompt/tools, and rehearse rollback.
3. **#58 live leg** — after #60, grow the profile's `brokered` list (a reviewed store change), `scripts/expand.py status` → `rehearse` → fresh report → `apply` ([`docs/runbooks/expansion.md`](runbooks/expansion.md)).
4. Run the full suite plus `tests/hermes_adapter/run_shadow_proof.py` and `tests/hermes_adapter/run_expansion_proof.py`, then `code-review` (Standards + Spec), then commit on `main` — the pattern used for #45–#60.

## Definition of done

#23 is done: the Stage 9 retirement **process** is recorded as ADR-0023 and [`docs/retirement/README.md`](retirement/README.md) — retirement is a reversible, batched, manifest-backed archive gated on Adam's recorded approval; deletion is a separate, later, explicitly-approved purge; the review artifact (`docs/retirement/<batch>.json` + `report.md`) and its generated evidence are defined; a deterministic `scripts/retire.py propose` proposes candidates (never a model); the curator keeps sole ownership of its managed roots and Stage 9 never touches `.archive/`/`.curator_backups/`; and the blocking conditions (uncut Consumer, open Incident, failing validation or stale digest, unresolved/unverified candidate, use inside the retention window, dependency/cross-skill/cron/pin reference, unmet licence/NOTICE) are listed. `CONTEXT.md` now distinguishes Retirement from Purge. Execution stays blocked until every Consumer resolves through the store.

#58 is done: the expansion procedure is recorded ([`docs/runbooks/expansion.md`](runbooks/expansion.md), ADR-0022) and enforced in `scripts/broker/expansion.py`; a second batch (`stillroom-signal-generator-brokered-2`, the pilot allowlist plus three newly-brokered stillroom workflows) is enabled end to end on fresh replay evidence with its own review in `tests/hermes_adapter/run_expansion_proof.py` (`VERDICT: PASS`), without changing the first batch's grant or Pack hash; the Soft Thresholds are held unchanged and a re-derivation is accepted only against a strictly larger corpus, recording the corpus digest and baseline; the running batch's rollback is rehearsed before enabling and restores the previous exposure; the post-batch foundation-resolution and hash checks are reported; and a profile with an open Incident fails expansion closed. Covered by `tests/test_broker_expansion.py` (32 tests) and `tests/test_expansion_proof.py` (7 tests).

#60 is in progress (not closed): the live-path blockers are fixed and pushed — the Shadow Report now joins a live Decision to its turn by request content hash (verified live: 0 → 5 turns), and Recordings can be frozen from the recorded Judgment (`record --from-evidence`) or, for a pre-broker Case the broker never judged, by asking live Jev (`record --live`, `extract_cases` backfilling `request_sha256` on old Cases so the join works); live Jev is a direct HTTP call whose key resolves through Hermes's per-turn `agent.secret_scope` (fixed this session: a routed gateway/desktop/kanban turn keeps credentials out of `os.environ`, so an env-only read silently fell back to No-Skill on exactly the observed turns); and `scripts/corpus.py queue` writes a machine-local, live-Jev pre-labelled review queue. Remaining: accumulate ≥20 threshold-side Cases (18 today), hand-review the queue (Recordings for all 27 already frozen with `record --live`), `evaluate.py register`, then build + review the Shadow Report.

#53 is done: spec B16's open decision is recorded as ADR-0021 — both options assessed against
the live `stillroom-signal-generator` configuration, Cutover `skills.disabled` chosen, hard-gate
item 5′ placed at Cutover and checked per batch, the Adapter's packaging carried as fog, and
reversibility recorded — and implemented in `scripts/cutover.py` (withholding applied and
gate-checked against the withheld index, byte-exact rollback), five new tests in
`tests/test_cutover.py`, and a real byte-copy of the pilot's config applied, verified and rolled
back byte-for-byte.

#56 is done: all six acceptance criteria are covered by `tests/test_broker_report.py` (27 tests)
— the report assembled from recorded Route Decisions plus observed skill use over a bounded
window, the profile and exact Brokered Allowlist batch named, each Hard Gate's status and
Soft-Threshold movement reported, attributed disagreements against actual use and human review,
the review (approve / reject / approve-narrower) as the only path to the injection switch and
bound to the report digest, and a rejection enabling nothing while Shadow Mode runs. The full
368-test suite passes, and the change is reviewed on both axes before committing on `main`.
