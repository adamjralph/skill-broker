# Agent Instructions

## Project

Skill Broker is a deterministic skill-intervention layer for Hermes agents. The
authoritative description is [`PROJECT-OUTLINE.md`](./PROJECT-OUTLINE.md); see
[`README.md`](./README.md) for orientation.

## Agent skills

### Issue tracker

Issues and specs are tracked as GitHub issues on `adamjralph/skill-broker`; run `gh` with `GH_CONFIG_DIR=~/.config/gh-personal`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
